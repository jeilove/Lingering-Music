# CHANGELOG

## [2026-04-05]
### 추가됨
- **음원 다운로드 기능 복구**: 프론트엔드 UI(검색 결과, 플레이어, 라이브러리)에서 음원 파일을 MP3로 즉시 다운로드할 수 있는 버튼을 다시 활성화했습니다. 
- **다운로드 API 연동**: 서버의 `yt-dlp` 기반 음원 추출 엔드포인트를 프론트엔드 `downloadTrack` 유틸리티와 연결했습니다.

## [2026-03-21] - 데이터 저장소 전면 개편 및 성능 최적화
### 추가됨
- **다중 즐겨찾기 선택 (Multi-select)**: 한 곡을 여러 즐겨찾기 그룹에 동시에 담을 수 있는 기능을 구현했습니다. 기존의 '이동' 방식에서 '체크박스' 토글 방식으로 UI를 개선했습니다.
- **추천 엔진 v2.0**: 상위 8개 태그 분석, 상위 5인 아티스트 가중치, 즐겨찾기 곡 가중치(3배)를 결합하여 훨씬 다양하고 풍부한 추천 리스트를 제공합니다. 곡이 부족할 경우 자동 보충 로직도 포함되었습니다.

### 수정됨
- **네트워크 병목 및 'ERR_CONNECTION_REFUSED' 해결**: 
  - 좀비 프로세스를 정리하고, 브라우저의 수백 개 병렬 요청을 서버 사이드 일괄 처리(`migrateBatch`, `getRecentTracks`)로 통합하여 통신량을 90% 이상 감축했습니다.
  - `localhost` 대신 안정적인 `127.0.0.1` 주소를 강제하여 윈도우 환경에서의 연결 안정성을 확보했습니다.
- **백엔드 하드디스크 저장소 완성**: 모든 데이터를 `apps/server/data/db.json` 하나로 통합하여 브라우저 독립성을 완벽히 확보했습니다. 
- **서버 안정성 및 방어 코드**: 
  - TypeScript 컴파일 에러(2769)를 수정(Port Number 캐스팅 등)하여 서버 구동 신뢰도를 높였습니다.
  - 유튜브 정보 추출 시 발생할 수 있는 데이터 누락 상황에 대비한 `Defensive Coding`을 적용하여 서버 크래시를 방지했습니다.
  - Chrome의 보안 정책(Private Network Access)을 고려한 전용 CORS 헤더를 추가했습니다.

## [2026-03-20] - 재생 엔진 고도화 및 UX 편의성 강화
### 추가됨
- **개별 곡 삭제 및 제외 기능**: 최근 재생 목록(Library)과 추천 목록(Home)의 노래 카드에 '삭제' 버튼(휴지통 아이콘)을 추가. 오류가 난 곡이나 원치 않는 추천곡을 즉시 제거할 수 있는 기능을 구현했습니다.
- **서버 사이드 캐싱 (1시간)**: 유튜브 검색 결과를 서버 메모리에 캐싱하여, 동일 키워드 재검색 시의 응답 속도를 0.1초 내외로 단축했습니다.

### 수정됨
- **로컬 파일 저장소 도입 (브라우저 종속 탈피)**: 불안정했던 브라우저 DB(IndexedDB) 방식을 버리고, 서버 하드디스크(`apps/server/data/db.json`)에 직접 데이터를 저장하는 방식으로 전면 교체했습니다. 이제 포트를 변경하거나 브라우저를 초기화해도 데이터가 절대 사라지지 않습니다.
- **데이터 자동 마이그레이션**: 앱 실행 시 기존 브라우저에 저장되어 있던 재생 기록과 즐겨찾기 정보를 감지하여, 자동으로 새로운 로컬 파일 서버로 안전하게 옮겨주는 로직을 구현했습니다.
- **검색 결과 앨범 커버(썸네일) 누락 해결**: `--flat-playlist` 검색 시 유튜브 엔진이 썸네일 URL을 정상 추출하지 못해 빈 화면(엑박/로고)만 나오던 현상을 수정했습니다. 고유 ID를 활용한 유튜브 기본 썸네일 경로(`${id}/hqdefault.jpg`) 생성 로직을 추가하여 모든 곡에 선명한 이미지가 출력되도록 복구했습니다.
- **한글 파손 현상(인코딩) 해결**: 윈도우 환경에서 `yt-dlp`가 검색 결과를 반환할 때 발생하던 글자 깨짐(Mojibake) 오류를 해결하기 위해 내부 파이썬 엔진에 강제 UTF-8 인코딩(`PYTHONIOENCODING='utf8'`)을 적용했습니다. 
- **AI 추천곡 재생 오류 해결**: 유튜브 검색 시 누락된 정보(ID 부족, 썸네일 없음 등)를 `NA`로 잘못 매칭하여 브라우저에서 404 에러가 발생하고 정상 재생되지 않던 문제를 예외 처리하여 해결했습니다.
- **트랙 전환 버그 해결**: 새 노래 선택 시 이전 노래가 겹쳐 들리던 현상(Overlap)을 완전히 해결했습니다. 새 곡 로드 즉시 기존 재생을 중단하고 오디오 소스를 초기화하는 로직을 적용했습니다.
- **검색 속도 대폭 개선**: 유튜브 검색 시 상세 정보 수집 단계를 생략하는 `--flat-playlist` 기법을 적용하여 검색 결과 배출 속도를 3~5배 향상시키고, 초기 결과 수를 15개로 최적화했습니다.
- **데이터 안정성 복구**: IndexedDBProvider의 비정상적인 구문 오류를 수정하고 데이터 정합성을 확보했습니다.

## [2026-03-19] - UI 안정화 및 데이터 지속성 강화
### 추가됨
- **TrackCard & CompactTrackItem 통합**: 앱 전반의 노래 카드 디자인을 통일하고, **로컬 이미지 폴백 시스템**을 도입하여 이미지 엑박 문제를 근본적으로 해결.
- **YouTube 기반 AI 추천**: MusicBrainz 대신 유튜브 검색 API(`/api/yt-search`)를 사용하여 고화질 썸네일과 정확한 추천곡 리스트를 제공.
- **상태 유지(Persistence)**: 사용자가 선택한 **태그와 즐겨찾기 그룹**을 `localStorage`에 저장하여 새로고침(F5) 후에도 선택 상태가 유지되도록 기능 강화.
- **초기화 로직 최적화**: Zustand 스토어 초기화 시 데이터를 일괄 처리(Batching)하여 화면 깜박임(Flickering) 현상 제거.
- **로딩 스켈레톤 UI**: AI 추천 섹션 로딩 시 레이아웃 무너짐 방지를 위한 애니메이션 스켈레톤 추가.

### 수정됨
- **서버 업그레이드**: `yt-dlp`를 활용한 실시간 유튜브 검색 엔드포인트 추가.
- **이미지 로딩 최적화**: 불안정한 외부 플레이스홀더(`via.placeholder.com`)를 제거하고 안정적인 내장 아이콘 데이터로 대체.
- **런타임 오류 수정**: `store.setState` 및 `store.loadAIRecommendations` 관련 호출 오류 해결.
