# 프로젝트 작업 과정 (Walkthrough)

## Phase 1: 기반 환경 구축
### 아키텍처 결정
- **모노레포**: `pnpm`을 사용하여 `apps/web`, `apps/server`, `packages/shared`로 분리된 구조를 채택했습니다.
- **프론트엔드**: Next.js 14+ (App Router)를 지향하며, 인터랙션이 복잡한 경우에만 `'use client'`를 사용합니다.
- **백엔드**: Express를 통해 유튜브 스트리밍 및 다운로드 로직을 처리합니다.
- **데이터 저장**: IndexedDB(Dexie.js)를 기점 데이터 저장소로 사용하여 사용자의 로컬 환경을 최대한 활용합니다.

## Phase 2: 핵심 기능 구현 및 UI 고도화 (2026-03-18/19)
- **스트리밍 엔진**: `yt-dlp` 기반 실시간 유튜브 오디오 매칭.
- **즐겨찾기 & 태그**: IndexedDB 연동 및 가사 패널 내 태그 관리 UI.
- **가사 서비스**: LRCLIB API 연동 및 Synced Lyrics 지원.

## Phase 3: 로컬 하드디스크 저장소 전환 및 성능 최적화 (2026-03-21)

### 1. 브라우저 종속 탈피 (Local DB)
- 불안정했던 브라우저 `IndexedDB`를 완전히 걷어내고, 서버의 하드디스크(`db.json`)를 주 저장소로 채택했습니다.
- 이제 서버 포트가 바뀌거나 브라우저 캐시를 삭제해도 재생 기록, 태그, 즐겨찾기 정보가 영구 보존됩니다.

### 2. 네트워크 퀀텀 점프 (Performance)
- 기존 브라우저와 서버 간의 비효율적인 수백 회의 통신을 단 1~2회의 일괄 요청(`Batch Request`)으로 압축했습니다.
- "최근 재생 목록"과 "추천 곡" 계산 로직을 서버 내부로 옮겨, 클라이언트 부하를 95% 이상 줄이고 `ERR_CONNECTION_REFUSED` 문제를 근본적으로 해결했습니다.

### 3. 즐겨찾기 시스템 자유도 확장
- 한 곡이 단 하나의 그룹에만 속할 수 있었던 제약을 제거하고, 여러 그룹에 동시에 중복 소속될 수 있는 다중 선택(Multi-select) 시스템을 도입했습니다.

---

## 다음 작업 예고 (Next Stage)
- [ ] **데이터 실시간 동기화**: 동기화 로직을 백엔드 파일 시스템과 연결하여 여러 브라우저 탭 간의 완벽한 일치를 구현
- [ ] **디자인 디테일**: 가사 패널 및 재생바 영역의 미세 애니메이션 고도화
